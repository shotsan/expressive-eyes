{
  "name": "expressive-eyes",
  "version": "0.1.0",
  "description": "Designed character eyes with emotions, comparing two battle-tested runtimes: Rive (interactive state machine) and Lottie (designer-made animation clips).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "@rive-app/canvas": "^2.21.6",
    "lottie-web": "^5.12.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
